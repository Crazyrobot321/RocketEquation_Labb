name: Secrets Demo

on:
  workflow_dispatch:

jobs:
  repo-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Print the secret on purpose
        env:
          API_KEY: ${{ secrets.DEMO_API_KEY }}
        run: echo "They key is $API_KEY"
